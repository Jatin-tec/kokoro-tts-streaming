# Host port mapped to the container's internal port 5000
HOST_PORT=5001

# Number of uvicorn workers (1 is safest for GPU; increase for CPU)
WORKERS=1

# HuggingFace cache dir inside the container (backed by a named volume)
HF_HOME=/app/hf_cache
